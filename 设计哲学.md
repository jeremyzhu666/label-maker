# Label Maker 设计哲学

> 工具即工具，而非表演。功能软件应如测量仪器般克制，而非消费品般炫技。

---

## 一、秩序美学

**核心理念：统一来源，一处改全局生效。**

### 1.1 尺寸变量化

所有硬编码尺寸抽取为 CSS 变量，集中在 `:root` 定义：

```css
:root{
  --control-h:40px;        /* 输入框/按钮/stepper 统一高度 */
  --radius:4px;             /* 4px 微圆角（专业冷静） */
  --s-1:4px; --s-2:8px;    /* 4px 基线网格 */
  --text-sm:13px;           /* 5 档阶字 */
}
```

**原则：** 任何出现两次以上的数值，必须变量化。一处改，全局生效。

### 1.2 控件统一

- 输入框、按钮、stepper 高度统一 40px（桌面）/ 38px（移动）
- 边框统一 `--border-strong`（#d4d4d4）
- 圆角统一 4px
- 字号统一 13px

**原则：** 同类控件必须视觉同构。用户不应通过尺寸猜测重要性。

### 1.3 布局对齐

- 编辑区与预览区严格等高（`--editor-height:250px`）
- 上边沿严格对齐（`align-items:stretch`）
- 4px 基线网格贯穿全局间距

**原则：** 对齐是秩序的最低要求。错位的元素破坏整体感。

---

## 二、简约美学

**核心理念：内容优先，装饰后退。**

### 2.1 去装饰

- 移除预览区左侧品牌色装饰条
- 移除区域标题前的编号（01/02/03）
- 移除框中框嵌套
- 移除版本号显示（纯前端工具无需版本管理）
- 移除技术信息模块（面向用户，不展示实现细节）

### 2.2 去冗余

- 移除保存功能（自动持久化 + 防抖）
- 移除 Docsify 站点（GitBook 托管）
- 移除连接引导文案
- 移除连接成功后的绿色背景

### 2.3 扁平无阴影

```css
.step{
  background:var(--card);
  border:1px solid var(--border);  /* 靠边框，不靠阴影 */
  border-radius:var(--radius);
}
```

**原则：** 阴影是装饰，边框是结构。用结构表达层次，而非装饰。

---

## 三、一致性美学

**核心理念：同类同构，全局统一。**

### 3.1 标点规范

- 中文文案：全角标点（，。：；）
- 英文文案：纯半角 ASCII（`. , - x ...`）
- `·` → `-`，`×` → `x`，`…` → `...`

**原则：** 标点是语言的节奏。中英文标点混用破坏阅读流畅。

### 3.2 命名统一

- 工具正式名称：Label Maker 标签生成工具
- 副标题：developed by 酷卡创新
- 按钮文案：连接打印机、立即打印

### 3.3 状态集成

- 状态点集成进按钮（`.btn-connect` 内含 `.status-dot`）
- 状态文案与按钮对齐
- 去除独立状态框

**原则：** 相关信息聚集，无关信息分离。状态与动作同框，降低视觉跳转。

---

## 四、字体美学

**核心理念：英文 Barlow，中文 PingFang SC，绝不混用。**

### 4.1 字体分流

```js
family:    '"Barlow Condensed","PingFang SC",sans-serif'  // 英文优先
familyCJK: '"PingFang SC",sans-serif'                      // 中文专用
```

- 英文内容用 Barlow Condensed（热敏打印笔画不粘连）
- 中文内容用 PingFang SC（系统原生中文字体）
- `drawTextClip` 根据 `/[\u4e00-\u9fa5]/` 判断文字是否含中文，选择字体

### 4.2 异步加载顺序

```
页面加载 → canvas 白底 + 中文（PingFang SC）立即显示
         ↓
         英文内容 skip（空白）
         ↓
Barlow 加载完 → 英文用 Barlow 渲染
```

- `englishReady` 标志控制英文渲染时机
- Barlow 未加载时英文留空，**绝不 fallback 到 PingFang SC**
- `FontFace.status === 'loaded'` 检测（比 `fonts.check` 更可靠）

**原则：** 英文只有空白或 Barlow 两种状态。宁可空白等待，不可错误渲染。

### 4.3 UI 字体

```css
--font-ui:'PingFang SC','Hiragino Sans GB','Microsoft YaHei','SimHei',sans-serif;
```

- 系统原生中文字体优先（PingFang SC / 微软雅黑 / 黑体）
- 不引入外部 UI 字体（零加载延迟）

---

## 五、性能美学

**核心理念：缓存一切重复计算，渲染最小化。**

### 5.1 字体渲染优化

- `fontOf` 全局缓存：同一 `weight+size+isCJK` 只拼接一次字符串
- `measureCache` 两级对象：`measureCache[size][text] = width`，命中时零拼接
- 快速路径：原字号能放下时跳过二分查找
- 二分查找最大能放下的字号（O(log n)）

### 5.2 脏区绘制

- `dirtyCells` Set 跟踪变更单元格
- 只重绘变更区域，不全量重绘
- `measureCache` 跨帧复用（整体重建替代逐 key delete）

### 5.3 预计算

- 布局常量预计算（`CELL_X`、`CELL_Y`、`BW`、`BH`）
- `taskSlots` 预分配 4 个静态槽位，避免每次 draw 重建数组
- 事件委托替代逐元素绑定

### 5.4 加载策略

- Barlow CSS 用 `media="print" onload="this.media='all'"` 异步加载
- `preconnect` 提前建立 CDN 连接
- niimbot.js 用 `async` 不阻塞首屏

**原则：** 用户输入响应必须 < 16ms（60fps）。字体加载不可阻塞渲染。

---

## 六、交互美学

**核心理念：即时反馈，无突兀跳变。**

### 6.1 输入防抖

- `saveTitles` 防抖持久化
- `beforeunload` 数据保护
- 输入长度限制（标题 12 字，内容 16 字）

### 6.2 移动端适配

- 双栏 → 单列回退（`display:contents` 跨容器排序）
- 打印按钮上下排列（单列 grid）
- 控件高度降至 38px
- 禁止双击缩放（`-webkit-tap-highlight-color:transparent`）
- 禁止输入聚焦自动放大（`maximum-scale=1.0`）

### 6.3 无闪烁

- canvas 无 `opacity` 渐变（直接显示白色背景）
- 语言切换按钮无 `transition`（HTML 预写 `active` 类）
- 字体加载不切换显示状态（空白 → Barlow，无中间态）

**原则：** 任何视觉跳变都是体验缺陷。状态变化要么即时，要么不可见。

---

## 总结

| 美学维度 | 核心原则 | 实现要点 |
|---------|---------|---------|
| 秩序美学 | 统一来源 | CSS 变量化、控件同构、对齐严格 |
| 简约美学 | 内容优先 | 去装饰、去冗余、扁平无阴影 |
| 一致性美学 | 同类同构 | 标点规范、命名统一、状态集成 |
| 字体美学 | 中英分流 | Barlow + PingFang SC、异步加载、绝不 fallback |
| 性能美学 | 缓存至上 | fontOf 缓存、measureCache、脏区绘制 |
| 交互美学 | 即时反馈 | 防抖、移动端适配、无闪烁 |

**设计信条：**

> 克制即专业。最少色彩、最微圆角、最扁平设计。
> 内容即设计。信息优先于装饰。
> 一致即美。同类元素必须同构。
